{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/loadStatusEnumSet.schema.json",
  "title": "loadStatusEnumSet",
  "description": "Load Status",
  "oneOf": [
    {
      "enum": [
        0,
        "loadUnknown"
      ],
      "description": "Never shed by DLLR"
    },
    {
      "enum": [
        1,
        "loadShed"
      ],
      "description": "Shed"
    },
    {
      "enum": [
        2,
        "loadReleased"
      ],
      "description": "Released"
    },
    {
      "type": "null"
    }
  ]
}