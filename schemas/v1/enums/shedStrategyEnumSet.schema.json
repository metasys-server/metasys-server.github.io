{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/shedStrategyEnumSet.schema.json",
  "title": "shedStrategyEnumSet",
  "description": "Shed Strategy",
  "oneOf": [
    {
      "enum": [
        0,
        "shedStratDllr"
      ],
      "description": "Demand Limiting and Load Rolling"
    },
    {
      "enum": [
        1,
        "shedStratDl"
      ],
      "description": "Demand Limiting Only"
    },
    {
      "enum": [
        2,
        "shedStratLr"
      ],
      "description": "Load Rolling Only"
    }
  ]
}