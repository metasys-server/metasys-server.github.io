{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/snrTypeEnumSet.schema.json",
  "title": "snrTypeEnumSet",
  "description": "SNR Type",
  "oneOf": [
    {
      "enum": [
        0,
        "snrSystemAlert"
      ],
      "description": "System Alert"
    },
    {
      "enum": [
        1,
        "snrOperatorTransaction"
      ],
      "description": "Operator Transaction"
    },
    {
      "enum": [
        2,
        "snrAlarm"
      ],
      "description": "Alarm"
    }
  ]
}