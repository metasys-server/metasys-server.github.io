{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/identificationModeEnumSet.schema.json",
  "title": "identificationModeEnumSet",
  "description": "Identification Mode",
  "oneOf": [
    {
      "enum": [
        0,
        "identificationModeRegularAccess"
      ],
      "description": "Regular Access"
    },
    {
      "enum": [
        1,
        "identificationModeValidationOnly"
      ],
      "description": "Validation Only"
    },
    {
      "enum": [
        2,
        "identificationModeTrackingOnly"
      ],
      "description": "Tracking Only"
    }
  ]
}