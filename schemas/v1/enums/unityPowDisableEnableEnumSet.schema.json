{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/unityPowDisableEnableEnumSet.schema.json",
  "title": "unityPowDisableEnableEnumSet",
  "description": "Unity POW Disable/Enable",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "ENABLE"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "DISABLE"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "MANUAL, ENABLE"
    },
    {
      "enum": [
        3,
        "3"
      ],
      "description": "MANUAL, DISABLE"
    },
    {
      "enum": [
        4,
        "4"
      ],
      "description": "RELEASE"
    }
  ]
}