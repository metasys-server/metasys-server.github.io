{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/seMaOpp.schema.json",
  "title": "seMaOpp",
  "description": "SE RTU Mixed Air Operation State",
  "oneOf": [
    {
      "enum": [
        0,
        "seMaOppTstatOnly"
      ],
      "description": "Tstat Only"
    },
    {
      "enum": [
        1,
        "seMaOppDat"
      ],
      "description": "DAT Control"
    },
    {
      "enum": [
        2,
        "seMaOppZone"
      ],
      "description": "Zone Control"
    }
  ]
}