{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/bacnetDnssdModeEnumSet.schema.json",
  "title": "bacnetDnssdModeEnumSet",
  "description": "BACnet DNS-SD Mode",
  "oneOf": [
    {
      "enum": [
        0,
        "bacnetDnssdModeMdnsOnly"
      ],
      "description": "MDNS Only"
    },
    {
      "enum": [
        1,
        "bacnetDnssdModeStayOnDns"
      ],
      "description": "Stay On DNS Server"
    },
    {
      "enum": [
        2,
        "bacnetDnssdModeFallback"
      ],
      "description": "Fallback"
    },
    {
      "enum": [
        3,
        "bacnetDnssdModeDnsOnly"
      ],
      "description": "DNS Server Only"
    },
    {
      "enum": [
        4,
        "bacnetDnssdModeBoth"
      ],
      "description": "Both"
    }
  ]
}