{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/localRemoteEnumSet.schema.json",
  "title": "localRemoteEnumSet",
  "description": "Local/Remote",
  "oneOf": [
    {
      "enum": [
        0,
        "locremLocal"
      ],
      "description": "Local"
    },
    {
      "enum": [
        1,
        "locremRemote"
      ],
      "description": "Remote"
    }
  ]
}     2,
        "2"
      ],
      "description": "Hold"
    }
  ]
}