{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/ostTypeEnumSet.schema.json",
  "title": "ostTypeEnumSet",
  "description": "OST Type",
  "oneOf": [
    {
      "enum": [
        0,
        "ostHeatingCooling"
      ],
      "description": "Heating / Cooling"
    },
    {
      "enum": [
        1,
        "ostHeatingOnly"
      ],
      "description": "Heating Only"
    },
    {
      "enum": [
        2,
        "ostCoolingOnly"
      ],
      "description": "Cooling Only"
    }
  ]
}