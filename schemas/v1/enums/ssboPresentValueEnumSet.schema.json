{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/ssboPresentValueEnumSet.schema.json",
  "title": "ssboPresentValueEnumSet",
  "description": "SSBO Present Value",
  "oneOf": [
    {
      "enum": [
        0,
        "ssboInactive"
      ],
      "description": "Inactive"
    },
    {
      "enum": [
        1,
        "ssboActive"
      ],
      "description": "Active"
    },
    {
      "enum": [
        2,
        "ssboUnknown"
      ],
      "description": "Unknown"
    }
  ]
}