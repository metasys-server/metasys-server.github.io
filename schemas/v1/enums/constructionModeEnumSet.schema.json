{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/constructionModeEnumSet.schema.json",
  "title": "constructionModeEnumSet",
  "description": "Construction Mode",
  "oneOf": [
    {
      "enum": [
        0,
        "cmNotInUse"
      ],
      "description": "Not In Use"
    },
    {
      "enum": [
        1,
        "cmHeat"
      ],
      "description": "Heat"
    },
    {
      "enum": [
        2,
        "cmCool"
      ],
      "description": "Cool"
    },
    {
      "enum": [
        3,
        "cmFanOnly"
      ],
      "description": "Fan Only"
    }
  ]
}