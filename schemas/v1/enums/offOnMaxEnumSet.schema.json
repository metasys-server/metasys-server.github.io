{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/offOnMaxEnumSet.schema.json",
  "title": "offOnMaxEnumSet",
  "description": "Off/On/Max",
  "oneOf": [
    {
      "enum": [
        0,
        "maxOff"
      ],
      "description": "Off"
    },
    {
      "enum": [
        1,
        "maxOn"
      ],
      "description": "On"
    },
    {
      "enum": [
        2,
        "maxMax"
      ],
      "description": "Max"
    }
  ]
}