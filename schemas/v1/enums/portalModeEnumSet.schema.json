{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/portalModeEnumSet.schema.json",
  "title": "portalModeEnumSet",
  "description": "Portal Mode",
  "oneOf": [
    {
      "enum": [
        0,
        "portalModeNormal"
      ],
      "description": "Normal (Access Mode)"
    },
    {
      "enum": [
        1,
        "portalModeOverride"
      ],
      "description": "Override (Open Mode)"
    },
    {
      "enum": [
        2,
        "portalModeLockdown"
      ],
      "description": "Lockdown (Secure Mode)"
    },
    {
      "enum": [
        3,
        "portalModeUnknown"
      ],
      "description": "Unknown"
    }
  ]
}