{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/enableDisableEnumSet.schema.json",
  "title": "enableDisableEnumSet",
  "description": "Enable Disable",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "Enable"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "Disable"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "Hold"
    }
  ]
}