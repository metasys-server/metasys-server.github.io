{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/sssiPresentValueEnumSet.schema.json",
  "title": "sssiPresentValueEnumSet",
  "description": "SSSI Present Value",
  "oneOf": [
    {
      "enum": [
        0,
        "sssiSecure"
      ],
      "description": "Secure"
    },
    {
      "enum": [
        1,
        "sssiAlarm"
      ],
      "description": "Alarm"
    },
    {
      "enum": [
        2,
        "sssiShort"
      ],
      "description": "Short"
    },
    {
      "enum": [
        3,
        "sssiOpen"
      ],
      "description": "Open"
    },
    {
      "enum": [
        4,
        "sssiUnknown"
      ],
      "description": "Unknown"
    },
    {
      "enum": [
        5,
        "sssiNotInitialized"
      ],
      "description": "Not Initialized"
    }
  ]
}