{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/connectedStatusEnumSet.schema.json",
  "title": "connectedStatusEnumSet",
  "description": "Connected Status",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "Disconnected"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "Connecting"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "Authenticating"
    },
    {
      "enum": [
        3,
        "3"
      ],
      "description": "Connected"
    },
    {
      "enum": [
        4,
        "4"
      ],
      "description": "Remote Connected"
    },
    {
      "enum": [
        5,
        "5"
      ],
      "description": "Disconnecting"
    }
  ]
}