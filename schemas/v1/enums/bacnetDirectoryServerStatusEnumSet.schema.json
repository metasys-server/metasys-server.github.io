{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/bacnetDirectoryServerStatusEnumSet.schema.json",
  "title": "bacnetDirectoryServerStatusEnumSet",
  "description": "BACnet Directory Server Status",
  "oneOf": [
    {
      "enum": [
        0,
        "bacnetDirectoryServerUnknown"
      ],
      "description": "Unknown"
    },
    {
      "enum": [
        1,
        "bacnetDirectoryServerLocal"
      ],
      "description": "Local"
    },
    {
      "enum": [
        2,
        "bacnetDirectoryServerPrimary"
      ],
      "description": "Configured"
    },
    {
      "enum": [
        3,
        "bacnetDirectoryServerSecondary"
      ],
      "description": "Secondary"
    },
    {
      "enum": [
        4,
        "bacnetDirectoryNoServer"
      ],
      "description": "No Server"
    }
  ]
}