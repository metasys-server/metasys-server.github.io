{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/conditionEnumSet.schema.json",
  "title": "conditionEnumSet",
  "description": "Condition",
  "oneOf": [
    {
      "enum": [
        0,
        "matchAll"
      ],
      "description": "Match All (AND)"
    },
    {
      "enum": [
        1,
        "matchAny"
      ],
      "description": "Match Any (OR)"
    },
    {
      "enum": [
        2,
        "matchComplex"
      ],
      "description": "Complex"
    }
  ]
}