{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/tecRemotePulsedHeat.schema.json",
  "title": "tecRemotePulsedHeat",
  "description": "TEC Remote Pulsed Heat",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "Off"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "On"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "Occupancy Output"
    }
  ]
}