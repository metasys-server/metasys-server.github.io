{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/activeNodeTableStatusEnumSet.schema.json",
  "title": "activeNodeTableStatusEnumSet",
  "description": "Active Node Table Status",
  "oneOf": [
    {
      "enum": [
        0,
        "tokenRingMonitoringDisabled"
      ],
      "description": "Token Ring Monitoring Disabled"
    },
    {
      "enum": [
        1,
        "tokenRingStatusPending"
      ],
      "description": "Token Ring Status Pending"
    },
    {
      "enum": [
        2,
        "excludedFromTokenRing"
      ],
      "description": "Excluded From Token Ring"
    },
    {
      "enum": [
        3,
        "onlyMasterInTokenRing"
      ],
      "description": "Only Master In Token Ring"
    },
    {
      "enum": [
        4,
        "participatingInTokenRing"
      ],
      "description": "Participating In Token Ring"
    },
    {
      "enum": [
        5,
        "excludedDueToDuplicateNodeAddress"
      ],
      "description": "Excluded Due To Duplicate Node Address"
    }
  ]
}