{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/selfState.schema.json",
  "title": "selfState",
  "description": "Self Test State Selectors",
  "oneOf": [
    {
      "enum": [
        0,
        "selfStateStabilize"
      ],
      "description": "Stabilize"
    },
    {
      "enum": [
        1,
        "selfStateWait"
      ],
      "description": "Wait"
    },
    {
      "enum": [
        2,
        "selfStateCheck"
      ],
      "description": "Check"
    },
    {
      "enum": [
        3,
        "selfStateAvailable"
      ],
      "description": "Available"
    },
    {
      "enum": [
        4,
        "selfStateNotRun"
      ],
      "description": "Not Run"
    },
    {
      "enum": [
        5,
        "selfStatePass"
      ],
      "description": "Pass"
    },
    {
      "enum": [
        6,
        "selfStateWarning"
      ],
      "description": "Warning"
    },
    {
      "enum": [
        7,
        "selfStateFail"
      ],
      "description": "Fail"
    }
  ]
}