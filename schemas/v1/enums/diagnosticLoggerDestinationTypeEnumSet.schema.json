{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/diagnosticLoggerDestinationTypeEnumSet.schema.json",
  "title": "diagnosticLoggerDestinationTypeEnumSet",
  "description": "Diagnostic Logger Destination Types",
  "oneOf": [
    {
      "enum": [
        0,
        "diagnosticLoggerSerialDestinationType"
      ],
      "description": "Serial"
    },
    {
      "enum": [
        1,
        "diagnosticLoggerUsbFileDestinationType"
      ],
      "description": "USB File"
    },
    {
      "enum": [
        2,
        "diagnosticLoggerUserCardFileDestinationType"
      ],
      "description": "User Card File"
    },
    {
      "type": "null"
    }
  ]
}