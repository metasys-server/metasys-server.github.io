{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/enums/portStatusEnumSet.schema.json",
  "title": "portStatusEnumSet",
  "description": "Port Status Enum Set",
  "oneOf": [
    {
      "enum": [
        0,
        "psOffline"
      ],
      "description": "Unavailable"
    },
    {
      "enum": [
        1,
        "psOnline"
      ],
      "description": "Available"
    },
    {
      "enum": [
        2,
        "psError"
      ],
      "description": "Error"
    }
  ]
}