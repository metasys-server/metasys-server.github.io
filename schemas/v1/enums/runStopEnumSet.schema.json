{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/runStopEnumSet.schema.json",
  "title": "runStopEnumSet",
  "description": "Run Stop",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "Run"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "Stop"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "Hold"
    }
  ]
}