{
  "$schema": "http://json-schema.org/schema#",
  "$id": "https://metasys-server.github.io/schemas/v1/enums/unityPowClosedOpenEnumSet.schema.json",
  "title": "unityPowClosedOpenEnumSet",
  "description": "Unity POW Closed/Open",
  "oneOf": [
    {
      "enum": [
        0,
        "0"
      ],
      "description": "OPEN"
    },
    {
      "enum": [
        1,
        "1"
      ],
      "description": "CLOSED"
    },
    {
      "enum": [
        2,
        "2"
      ],
      "description": "MANUAL, OPEN"
    },
    {
      "enum": [
        3,
        "3"
      ],
      "description": "MANUAL, CLOSED"
    },
    {
      "enum": [
        4,
        "4"
      ],
      "description": "RELEASE"
    }
  ]
}