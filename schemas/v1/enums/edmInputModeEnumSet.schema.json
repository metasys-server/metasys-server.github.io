{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://metasys-server.github.io/schemas/enums/edmInputModeEnumSet.schema.json",
  "title": "edmInputModeEnumSet",
  "description": "Edm Input Modes",
  "oneOf": [
    {
      "enum": [
        0,
        "edmPulseMode"
      ],
      "description": "Pulse"
    },
    {
      "enum": [
        1,
        "edmAnalogMode"
      ],
      "description": "Analog"
    }
  ]
}